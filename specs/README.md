# Specs

This repository uses versioned specs under `specs/`.

- `specs/v0.1.0.md` is the active spec; it records the pipeline built so far and the work
  left to cut the first release. Nothing has been released yet, so there is no baseline spec.
- Tracked work in `planning/tasks/` links to its headings via `spec_ref`.

## Reading Order

1. `specs/v0.1.0.md`
