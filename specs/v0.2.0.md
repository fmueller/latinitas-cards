# LatinitasCards v0.2.0

## Summary

Planned release. Builds on v0.1.0's stable generated-note identity and deck-first CSV
workflow. Adds safe managed updates, Latin form parsing with calibrated review gates, and
the first configurable semantic color layer.

## Goals

- Update previously generated material without duplicate notes or lost review history.
- Protect user-owned fields and expose every conflict before applying changes.
- Generate high-precision form-parsing exercises soon after principal-part cards.
- Introduce presentation themes without coupling color values to linguistic data.

## Potential Features

### Managed Update Plans

Compare newly generated notes with a current Anki export and classify each logical identity
as create, update, unchanged, conflict, or retire. Plans must be deterministic,
machine-readable, and inspectable before application. A changed managed field conflicts when
the current value no longer matches the last generated value; user-owned personal fields do
not conflict because LatinitasCards never writes them.

### Safe Update Application

Apply an approved plan while retaining the existing logical note identity and Anki review
history. Updates affect only managed fields. Retired generated notes are tagged
`latinitas::retired` and suspended after explicit application; they are never silently
deleted. File-based output remains canonical. APKG support must follow Anki's GUID,
note-type, template, and modification-time semantics rather than manually treating local
note/card IDs as portable identity.

### Calibrated Form Parsing

Generate Latin-first parsing cards that map an encountered form to lemma, person, number,
tense, mood, voice, case, gender, or other applicable features. Confidence is tracked per
claim, not as one score for the whole analysis. CLTK/Stanza, deterministic rules, lexical
data, and optional LLM disambiguation are evidence sources; none is accepted as calibrated
without evaluation on reviewed material from the relevant Latin domain.

The default policy is precision-first for facts placed on study cards. High-confidence
claims may be applied automatically under project policy; ambiguous claims enter a review
queue with alternatives and provenance.

### Morphology Themes

Generated content uses semantic classes for roles such as lemma, stem, ending, noun, and
verb. A project theme maps those classes to colors and complementary visual cues. Theme
changes do not change note identity or rewrite linguistic annotations. Themes support light
and dark presentation and must not rely on color alone.

## Caution

- CSV, APKG, and direct collection updates have different matching behavior; a shared plan
  must not conceal transport-specific limitations.
- The installed CLTK/Stanza path does not provide calibrated token-level probabilities.
- LLM self-reported confidence is not calibration.
- Changing note-type or template identity can detach scheduling from its intended exercise.
- One generated exercise remains one note/card by default.

## Recommendation About LLM Support

Keep LLM use optional and network-explicit. It may propose alternatives, disambiguate in
context, or explain an analysis. Auto-application depends on measured recipe/analyzer
performance and project policy, not provider confidence claims.

## Explicitly Excluded

- Corpus-first generation.
- Syntactic sentence-role analysis and coloring.
- Direct updates to a running Anki instance.
- Automatic deletion of retired notes.
- Network-required core operation.
- Backwards-compatibility guarantees for pre-1.0 profiles and manifests.
